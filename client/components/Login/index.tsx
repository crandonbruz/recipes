import { Box } from "@mui/material";
import { styles } from "./styles";

export const LoginComp = () => {
  const { root } = styles;
  return (
    <Box sx={root}>
      <h1>Login</h1>
    </Box>
  );
};
