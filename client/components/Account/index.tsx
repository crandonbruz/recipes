import { Box } from "@mui/material";
import { styles } from "./styles";

export const AccountComp = () => {
  const { root } = styles;
  return (
    <Box sx={root}>
      <h1>Account</h1>
    </Box>
  );
};
