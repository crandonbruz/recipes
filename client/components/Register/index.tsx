import { Box } from "@mui/material";
import { styles } from "./styles";

export const RegisterComp = () => {
  const { root } = styles;
  return (
    <Box sx={root}>
      <h1>Register</h1>
    </Box>
  );
};
