import { Box } from "@mui/material";
import { styles } from "./styles";

export const UsersComp = () => {
  const { root } = styles;
  return (
    <Box sx={root}>
      <h1>Users</h1>
    </Box>
  );
};
